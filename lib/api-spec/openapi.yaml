openapi: 3.1.0
info:
  title: Api
  version: 0.2.0
  description: PR/PO Approval System API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: users
  - name: companies
  - name: purchase-requests
  - name: approvals
  - name: purchase-orders
  - name: notifications
  - name: audit-logs
  - name: settings
  - name: dashboard

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/change-password:
    post:
      operationId: changePassword
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangePasswordRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /users:
    get:
      operationId: getUsers
      tags: [users]
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: search
          in: query
          schema:
            type: string
        - name: role
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserListResponse"

    post:
      operationId: createUser
      tags: [users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /users/{id}:
    get:
      operationId: getUserById
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

    put:
      operationId: updateUser
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

    delete:
      operationId: deleteUser
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /users/{id}/leave-balance:
    get:
      operationId: getUserLeaveBalance
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: year
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveBalance"

    put:
      operationId: updateUserLeaveBalance
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLeaveBalanceRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveBalance"

  /users/{id}/companies:
    get:
      operationId: getUserCompanies
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserCompanyAssignment"

    put:
      operationId: updateUserCompanies
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserCompaniesRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserCompanyAssignment"

  /companies:
    get:
      operationId: getCompanies
      tags: [companies]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Company"

    post:
      operationId: createCompany
      tags: [companies]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCompanyRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Company"

  /companies/{id}:
    put:
      operationId: updateCompany
      tags: [companies]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCompanyRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Company"

    delete:
      operationId: deleteCompany
      tags: [companies]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /purchase-requests:
    get:
      operationId: getPurchaseRequests
      tags: [purchase-requests]
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: status
          in: query
          schema:
            type: string
        - name: type
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequestListResponse"

    post:
      operationId: createPurchaseRequest
      tags: [purchase-requests]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePurchaseRequestRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /purchase-requests/{id}:
    get:
      operationId: getPurchaseRequestById
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

    put:
      operationId: updatePurchaseRequest
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePurchaseRequestRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /purchase-requests/{id}/submit:
    post:
      operationId: submitPurchaseRequest
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /purchase-requests/{id}/receive:
    post:
      operationId: receivePurchaseRequest
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceivePRRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /purchase-requests/{id}/receive-items:
    post:
      operationId: receivePartialItems
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiveItemsRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /purchase-requests/{id}/close-receiving:
    post:
      operationId: closeReceiving
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notes:
                  type: string
                  nullable: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /purchase-requests/{id}/receiving-records:
    get:
      operationId: getReceivingRecords
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReceivingRecord"

  /settings/company-leave:
    get:
      operationId: getCompanyLeaveSettings
      tags: [settings]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CompanyLeaveSetting"

  /settings/company-leave/{companyId}:
    put:
      operationId: updateCompanyLeaveSetting
      tags: [settings]
      parameters:
        - name: companyId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCompanyLeaveSettingRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanyLeaveSetting"

  /purchase-requests/{id}/vendor-attachments:
    get:
      operationId: getPRVendorAttachments
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PrVendorAttachment"

    post:
      operationId: addPRVendorAttachment
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddVendorAttachmentRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrVendorAttachment"

  /purchase-requests/{id}/vendor-attachments/{attachmentId}:
    delete:
      operationId: deletePRVendorAttachment
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: attachmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /purchase-requests/{id}/select-vendor:
    post:
      operationId: selectPRVendor
      tags: [purchase-requests]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SelectVendorRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequest"

  /receiving:
    get:
      operationId: getReceivingList
      tags: [purchase-requests]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceivingListResponse"

  /approvals:
    get:
      operationId: getApprovals
      tags: [approvals]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalListResponse"

  /approvals/{id}/approve:
    post:
      operationId: approvePR
      tags: [approvals]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovalActionRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Approval"

  /approvals/{id}/reject:
    post:
      operationId: rejectPR
      tags: [approvals]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovalActionRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Approval"

  /approval-rules:
    get:
      operationId: getApprovalRules
      tags: [approvals]
      parameters:
        - name: type
          in: query
          schema:
            type: string
            enum: [purchase, repair, leave]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApprovalRule"

    post:
      operationId: createApprovalRule
      tags: [approvals]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateApprovalRuleRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalRule"

  /approval-rules/{id}:
    put:
      operationId: updateApprovalRule
      tags: [approvals]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateApprovalRuleRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalRule"

    delete:
      operationId: deleteApprovalRule
      tags: [approvals]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /purchase-orders:
    get:
      operationId: getPurchaseOrders
      tags: [purchase-orders]
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderListResponse"

    post:
      operationId: createPurchaseOrder
      tags: [purchase-orders]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePurchaseOrderRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"

  /purchase-orders/{id}:
    get:
      operationId: getPurchaseOrderById
      tags: [purchase-orders]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"

    put:
      operationId: updatePurchaseOrder
      tags: [purchase-orders]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePurchaseOrderRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"

  /purchase-orders/{id}/issue:
    post:
      operationId: issuePurchaseOrder
      tags: [purchase-orders]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"

  /purchase-orders/{id}/receive:
    post:
      operationId: receivePurchaseOrder
      tags: [purchase-orders]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"

  /notifications:
    get:
      operationId: getNotifications
      tags: [notifications]
      parameters:
        - name: unread
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationListResponse"

  /notifications/{id}/read:
    put:
      operationId: markNotificationRead
      tags: [notifications]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Notification"

  /notifications/read-all:
    put:
      operationId: markAllNotificationsRead
      tags: [notifications]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /audit-logs:
    get:
      operationId: getAuditLogs
      tags: [audit-logs]
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: prId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditLogListResponse"

  /settings:
    get:
      operationId: getSettings
      tags: [settings]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"

    put:
      operationId: updateSettings
      tags: [settings]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSettingsRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"

  /dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardData"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required: [error]

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
      required: [success]

    LoginRequest:
      type: object
      properties:
        username:
          type: string
        password:
          type: string
      required: [username, password]

    LoginResponse:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/User"
        message:
          type: string
      required: [user]

    Company:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        code:
          type: string
        address:
          type: string
          nullable: true
        isActive:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, name, code, isActive, createdAt]

    CreateCompanyRequest:
      type: object
      properties:
        name:
          type: string
        code:
          type: string
        address:
          type: string
          nullable: true
      required: [name, code]

    UserCompanyAssignment:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: string
        companyId:
          type: string
        companyName:
          type: string
        department:
          type: string
      required: [id, userId, companyId, companyName, department]

    UpdateUserCompaniesRequest:
      type: object
      properties:
        assignments:
          type: array
          items:
            $ref: "#/components/schemas/UserCompanyInput"
      required: [assignments]

    UserCompanyInput:
      type: object
      properties:
        companyId:
          type: integer
        department:
          type: string
      required: [companyId, department]

    ChangePasswordRequest:
      type: object
      properties:
        currentPassword:
          type: string
        newPassword:
          type: string
      required: [currentPassword, newPassword]

    LeaveBalance:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        year:
          type: integer
        balanceDays:
          type: number
        carriedOverDays:
          type: number
        carriedOverExpiry:
          type: string
          nullable: true
        usedDays:
          type: number
        availableDays:
          type: number
        lastAccumulatedMonth:
          type: integer
        updatedAt:
          type: string
      required: [userId, year, balanceDays, carriedOverDays, usedDays, availableDays, lastAccumulatedMonth]

    UpdateLeaveBalanceRequest:
      type: object
      properties:
        year:
          type: integer
        balanceDays:
          type: number
          nullable: true
        carriedOverDays:
          type: number
          nullable: true
        usedDays:
          type: number
          nullable: true
      required: [year]

    CompanyLeaveSetting:
      type: object
      properties:
        id:
          type: integer
        companyId:
          type: integer
        companyName:
          type: string
        carryoverExpiryMonth:
          type: integer
        carryoverExpiryDay:
          type: integer
        maxCarryoverDays:
          type: integer
        accrualDaysPerMonth:
          type: number
      required: [companyId, carryoverExpiryMonth, carryoverExpiryDay, maxCarryoverDays, accrualDaysPerMonth]

    UpdateCompanyLeaveSettingRequest:
      type: object
      properties:
        carryoverExpiryMonth:
          type: integer
        carryoverExpiryDay:
          type: integer
        maxCarryoverDays:
          type: integer
        accrualDaysPerMonth:
          type: number
      required: [carryoverExpiryMonth, carryoverExpiryDay, maxCarryoverDays, accrualDaysPerMonth]

    ReceiveItemsRequest:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              prItemId:
                type: integer
              receivedQty:
                type: number
              notes:
                type: string
                nullable: true
            required: [prItemId, receivedQty]
        notes:
          type: string
          nullable: true
      required: [items]

    ReceivingRecord:
      type: object
      properties:
        id:
          type: integer
        prId:
          type: integer
        prItemId:
          type: integer
        itemName:
          type: string
        receivedQty:
          type: number
        receivedAt:
          type: string
        receivedBy:
          type: integer
        receivedByName:
          type: string
        notes:
          type: string
          nullable: true
      required: [id, prId, prItemId, itemName, receivedQty, receivedAt, receivedBy, receivedByName]

    User:
      type: object
      properties:
        id:
          type: integer
        username:
          type: string
        name:
          type: string
        email:
          type: string
          nullable: true
        department:
          type: string
        position:
          type: string
        role:
          type: string
          enum: [admin, user, approver, purchasing]
        superiorId:
          type: integer
          nullable: true
        superiorName:
          type: string
          nullable: true
        hiredCompanyId:
          type: integer
          nullable: true
        hiredCompanyName:
          type: string
          nullable: true
        isActive:
          type: boolean
        companies:
          type: array
          items:
            $ref: "#/components/schemas/UserCompanyAssignment"
        createdAt:
          type: string
          format: date-time
      required: [id, username, name, department, position, role, isActive, createdAt]

    UserListResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/User"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [users, total, page, limit]

    CreateUserRequest:
      type: object
      properties:
        hiredCompanyId:
          type: integer
          nullable: true
        username:
          type: string
        password:
          type: string
        name:
          type: string
        email:
          type: string
          nullable: true
        department:
          type: string
        position:
          type: string
        role:
          type: string
          enum: [admin, user, approver, purchasing]
        superiorId:
          type: integer
          nullable: true
        companies:
          type: array
          items:
            $ref: "#/components/schemas/UserCompanyInput"
      required: [username, password, name, department, position, role]

    UpdateUserRequest:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          nullable: true
        department:
          type: string
        position:
          type: string
        role:
          type: string
          enum: [admin, user, approver, purchasing]
        superiorId:
          type: integer
          nullable: true
        hiredCompanyId:
          type: integer
          nullable: true
        isActive:
          type: boolean
        password:
          type: string
        companies:
          type: array
          items:
            $ref: "#/components/schemas/UserCompanyInput"

    PrItem:
      type: object
      properties:
        id:
          type: integer
        prId:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        qty:
          type: number
        unit:
          type: string
        estimatedPrice:
          type: number
        totalPrice:
          type: number
      required: [id, prId, name, qty, unit, estimatedPrice, totalPrice]

    PrItemInput:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true
        qty:
          type: number
        unit:
          type: string
        estimatedPrice:
          type: number
      required: [name, qty, unit, estimatedPrice]

    PrVendorAttachment:
      type: object
      properties:
        id:
          type: integer
        prId:
          type: integer
        vendorName:
          type: string
        fileUrl:
          type: string
        quotedPrice:
          type: number
          nullable: true
        notes:
          type: string
          nullable: true
        uploadedBy:
          type: integer
        uploaderName:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, prId, vendorName, fileUrl, uploadedBy, uploaderName, createdAt]

    AddVendorAttachmentRequest:
      type: object
      properties:
        vendorName:
          type: string
        fileUrl:
          type: string
        quotedPrice:
          type: number
          nullable: true
        notes:
          type: string
          nullable: true
      required: [vendorName, fileUrl]

    SelectVendorRequest:
      type: object
      properties:
        vendorAttachmentId:
          type: integer
        finalQty:
          type: number
          nullable: true
        finalAmount:
          type: number
      required: [vendorAttachmentId, finalAmount]

    ReceivingItem:
      type: object
      properties:
        id:
          type: integer
        type:
          type: string
          enum: [pr, po]
        prId:
          type: integer
        prNumber:
          type: string
        prDescription:
          type: string
        requesterName:
          type: string
        department:
          type: string
        vendorName:
          type: string
          nullable: true
        totalAmount:
          type: number
        status:
          type: string
        poId:
          type: integer
          nullable: true
        poNumber:
          type: string
          nullable: true
      required: [id, type, prId, prNumber, prDescription, requesterName, department, totalAmount, status]

    ReceivingListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ReceivingItem"
        total:
          type: integer
      required: [items, total]

    PurchaseRequest:
      type: object
      properties:
        id:
          type: integer
        prNumber:
          type: string
        date:
          type: string
          format: date-time
        requesterId:
          type: integer
        requesterName:
          type: string
        department:
          type: string
        companyId:
          type: integer
          nullable: true
        companyName:
          type: string
          nullable: true
        type:
          type: string
          enum: [purchase, repair, leave]
        description:
          type: string
        status:
          type: string
          enum: [draft, waiting_approval, approved, vendor_selected, rejected, completed]
        totalAmount:
          type: number
        attachmentUrl:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/PrItem"
        approvals:
          type: array
          items:
            $ref: "#/components/schemas/Approval"
        vendorAttachments:
          type: array
          items:
            $ref: "#/components/schemas/PrVendorAttachment"
        currentApprovalLevel:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true
        leaveStartDate:
          type: string
          nullable: true
        leaveEndDate:
          type: string
          nullable: true
        leaveRequesterId:
          type: integer
          nullable: true
        leaveRequesterName:
          type: string
          nullable: true
        selectedVendorId:
          type: integer
          nullable: true
        selectedVendorName:
          type: string
          nullable: true
        vendorFinalQty:
          type: number
          nullable: true
        vendorFinalAmount:
          type: number
          nullable: true
        vendorSelectedByName:
          type: string
          nullable: true
        vendorSelectedAt:
          type: string
          nullable: true
        receivingStatus:
          type: string
          enum: [none, partial, closed]
        receivingRecords:
          type: array
          items:
            $ref: "#/components/schemas/ReceivingRecord"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, prNumber, date, requesterId, requesterName, department, type, description, status, totalAmount, items, approvals, vendorAttachments, receivingStatus, receivingRecords, createdAt, updatedAt]

    PurchaseRequestListResponse:
      type: object
      properties:
        purchaseRequests:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseRequest"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [purchaseRequests, total, page, limit]

    CreatePurchaseRequestRequest:
      type: object
      properties:
        type:
          type: string
          enum: [purchase, repair, leave]
        description:
          type: string
        companyId:
          type: integer
          nullable: true
        department:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/PrItemInput"
        notes:
          type: string
          nullable: true
        leaveStartDate:
          type: string
          nullable: true
        leaveEndDate:
          type: string
          nullable: true
        leaveRequesterId:
          type: integer
          nullable: true
      required: [type, description]

    UpdatePurchaseRequestRequest:
      type: object
      properties:
        type:
          type: string
          enum: [purchase, repair, leave]
        description:
          type: string
        companyId:
          type: integer
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/PrItemInput"
        notes:
          type: string
          nullable: true
        leaveStartDate:
          type: string
          nullable: true
        leaveEndDate:
          type: string
          nullable: true
        leaveRequesterId:
          type: integer
          nullable: true

    ReceivePRRequest:
      type: object
      properties:
        notes:
          type: string
          nullable: true

    Approval:
      type: object
      properties:
        id:
          type: integer
        prId:
          type: integer
        approverId:
          type: integer
        approverName:
          type: string
        level:
          type: integer
        status:
          type: string
          enum: [pending, approved, rejected]
        notes:
          type: string
          nullable: true
        actionAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, prId, approverId, approverName, level, status, createdAt]

    ApprovalListResponse:
      type: object
      properties:
        approvals:
          type: array
          items:
            $ref: "#/components/schemas/ApprovalWithPR"
        total:
          type: integer
      required: [approvals, total]

    ApprovalWithPR:
      type: object
      properties:
        id:
          type: integer
        prId:
          type: integer
        prNumber:
          type: string
        prType:
          type: string
        prDescription:
          type: string
        requesterName:
          type: string
        department:
          type: string
        totalAmount:
          type: number
        approverId:
          type: integer
        approverName:
          type: string
        level:
          type: integer
        status:
          type: string
          enum: [pending, approved, rejected]
        notes:
          type: string
          nullable: true
        actionAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, prId, prNumber, prType, prDescription, requesterName, department, totalAmount, approverId, approverName, level, status, createdAt]

    ApprovalActionRequest:
      type: object
      properties:
        notes:
          type: string
          nullable: true

    ApprovalRuleLevel:
      type: object
      properties:
        id:
          type: integer
        ruleId:
          type: integer
        level:
          type: integer
        approverId:
          type: integer
        approverName:
          type: string
        minAmount:
          type: number
          nullable: true
        maxAmount:
          type: number
          nullable: true
      required: [id, ruleId, level, approverId, approverName]

    ApprovalRuleLevelInput:
      type: object
      properties:
        level:
          type: integer
        approverId:
          type: integer
        minAmount:
          type: number
          nullable: true
        maxAmount:
          type: number
          nullable: true
      required: [level, approverId]

    ApprovalRule:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        companyId:
          type: integer
          nullable: true
        companyName:
          type: string
          nullable: true
        department:
          type: string
          nullable: true
        type:
          type: string
          enum: [purchase, repair, leave]
        levels:
          type: array
          items:
            $ref: "#/components/schemas/ApprovalRuleLevel"
        createdAt:
          type: string
          format: date-time
      required: [id, name, type, levels, createdAt]

    CreateApprovalRuleRequest:
      type: object
      properties:
        name:
          type: string
        companyId:
          type: integer
          nullable: true
        department:
          type: string
          nullable: true
        type:
          type: string
          enum: [purchase, repair, leave]
        levels:
          type: array
          items:
            $ref: "#/components/schemas/ApprovalRuleLevelInput"
      required: [name, type, levels]

    PoItem:
      type: object
      properties:
        id:
          type: integer
        poId:
          type: integer
        prItemId:
          type: integer
          nullable: true
        name:
          type: string
        qty:
          type: number
        unit:
          type: string
        negotiatedPrice:
          type: number
        totalPrice:
          type: number
      required: [id, poId, name, qty, unit, negotiatedPrice, totalPrice]

    PoItemInput:
      type: object
      properties:
        prItemId:
          type: integer
          nullable: true
        name:
          type: string
        qty:
          type: number
        unit:
          type: string
        negotiatedPrice:
          type: number
      required: [name, qty, unit, negotiatedPrice]

    PurchaseOrder:
      type: object
      properties:
        id:
          type: integer
        poNumber:
          type: string
        prId:
          type: integer
        prNumber:
          type: string
        supplier:
          type: string
        status:
          type: string
          enum: [draft, issued, receiving, received]
        totalAmount:
          type: number
        notes:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/PoItem"
        createdById:
          type: integer
        createdByName:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, poNumber, prId, prNumber, supplier, status, totalAmount, items, createdById, createdByName, createdAt, updatedAt]

    PurchaseOrderListResponse:
      type: object
      properties:
        purchaseOrders:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseOrder"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [purchaseOrders, total, page, limit]

    CreatePurchaseOrderRequest:
      type: object
      properties:
        prId:
          type: integer
        supplier:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/PoItemInput"
        notes:
          type: string
          nullable: true
      required: [prId, supplier, items]

    UpdatePurchaseOrderRequest:
      type: object
      properties:
        supplier:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/PoItemInput"
        notes:
          type: string
          nullable: true

    Notification:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        title:
          type: string
        message:
          type: string
        type:
          type: string
          enum: [approval_request, approved, rejected, received, info]
        isRead:
          type: boolean
        prId:
          type: integer
          nullable: true
        poId:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, userId, title, message, type, isRead, createdAt]

    NotificationListResponse:
      type: object
      properties:
        notifications:
          type: array
          items:
            $ref: "#/components/schemas/Notification"
        unreadCount:
          type: integer
        total:
          type: integer
      required: [notifications, unreadCount, total]

    AuditLog:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        userName:
          type: string
        action:
          type: string
        entityType:
          type: string
        entityId:
          type: integer
        details:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, userId, userName, action, entityType, entityId, createdAt]

    AuditLogListResponse:
      type: object
      properties:
        auditLogs:
          type: array
          items:
            $ref: "#/components/schemas/AuditLog"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [auditLogs, total, page, limit]

    Settings:
      type: object
      properties:
        poEnabled:
          type: boolean
        companyName:
          type: string
        currency:
          type: string
      required: [poEnabled, companyName, currency]

    UpdateSettingsRequest:
      type: object
      properties:
        poEnabled:
          type: boolean
        companyName:
          type: string
        currency:
          type: string

    DashboardData:
      type: object
      properties:
        pendingApprovals:
          type: integer
        myPendingPRs:
          type: integer
        pendingPOs:
          type: integer
        totalPRs:
          type: integer
        totalPOs:
          type: integer
        recentPRs:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseRequest"
        recentApprovals:
          type: array
          items:
            $ref: "#/components/schemas/ApprovalWithPR"
        prByStatus:
          type: array
          items:
            $ref: "#/components/schemas/StatusCount"
      required: [pendingApprovals, myPendingPRs, pendingPOs, totalPRs, totalPOs, recentPRs, recentApprovals, prByStatus]

    StatusCount:
      type: object
      properties:
        status:
          type: string
        count:
          type: integer
      required: [status, count]
